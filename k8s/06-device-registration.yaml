apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-registration-api
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: device-registration-api
  template:
    metadata:
      labels:
        app: device-registration-api
    spec:
      serviceAccountName: dependency-waiter
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      - name: wait-for-postgres
        image: bitnami/kubectl:latest
        command:
          - sh
          - -c
          - |
            until kubectl wait --for=condition=ready pod -l app=postgres -n demo --timeout=1s; do
              echo "waiting for postgres pod to become ready";
              sleep 2;
            done
      containers:
      - name: device-registration-api
        image: fkauff/device-registration-api:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: connection-string
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: ASPNETCORE_ENVIRONMENT
        resources:
          limits:
            cpu: "200m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: device-registration-api
  namespace: demo
spec:
  selector:
    app: device-registration-api
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
